import { NodeMoveFinalizeExecution } from './node-move-finalize.execution';
import { NodeMoveFinalizeValidator } from './node-move-finalize.validator';

export const NODE_MOVE_FINALIZE_PROVIDERS = [

  NodeMoveFinalizeExecution,

  NodeMoveFinalizeValidator,
];
